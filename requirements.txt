# Core dependencies for MateriaScout
gradio
mp-api
pandas

# Note: Specific versions can be pinned using "==", e.g., gradio==4.10.0
# For this project, we'll let pip pick the latest compatible versions.
# Dependent libraries like numpy, requests, etc., will be pulled in by the above.
# For testing (usually in a dev environment):
# unittest (standard library)
